import { Routes } from '@angular/router';

// Single-view dashboard: no routes today, but the router stays wired so
// drill-down views can be added without restructuring bootstrap.
export const routes: Routes = [];
